action (StartOrder) {
  description (__DESCRIPTION__)
  type (Search)
  collect {
    input (userRequest) {
      type (UserRequest)
      min (Required) max (One)
      // default-init {
      //   intent {
      //     goal : GetUserRequestCandidate
      //   }
      // }
    }
  }
  output (Order)
}
