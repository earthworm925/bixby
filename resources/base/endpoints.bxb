endpoints {
  action-endpoints {

    action-endpoint (StartOrder) {
      accepted-inputs (userRequest)
      local-endpoint (StartOrder.js)
    }
    action-endpoint (GetUserRequestCandidate) {
      accepted-inputs ()
      local-endpoint (GetUserRequestCandidate.js)
    }
  }
}
